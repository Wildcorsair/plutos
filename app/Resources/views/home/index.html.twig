{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-2 pull-right">
                <a href="{{ path('login') }}">Sign In</a>
                <a href="{{ path('register') }}">Sign Up</a>
            </div>
        </div>
        <div class="row">
            <div class="col-md-12">Welcome to Home</div>
        </div>
    </div>
{% endblock %}