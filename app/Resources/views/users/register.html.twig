{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-4 col-md-offset-3">
                {{ form_start(register) }}
                {{ form_widget(register) }}
                {{ form_end(register) }}
            </div>
        </div>
    </div>
{% endblock %}